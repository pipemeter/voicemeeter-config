[package]
name = "voicemeeter-config"
version = "0.1.0"
edition = "2024"
authors = ["PipeMeeter Team <dev@pipemeeter.org>"]
description = "Reading and writing Voicemeeter's XML settings dialect"
license = "Unlicense"
repository = "https://github.com/pipemeter/voicemeeter-config"

[lib]
name = "voicemeeter_config"
path = "src/lib.rs"

[dependencies]
roxmltree = "0.20"
log = "0.4"
quick-xml = "0.37"

[lints.rust]
unsafe_code = "forbid"
missing_debug_implementations = "warn"

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
cast_precision_loss = "allow"
cast_possible_truncation = "allow"
similar_names = "allow"
